# Reference workflow

The reference workflow is:

```text
workflows/QWEN35_LLM_Captioning.json
```

Import it by dragging it into ComfyUI. Make sure LM Studio is running before executing the workflow.
